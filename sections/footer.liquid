
<footer class="main-footer">
<div class="newsletter" style="--align: {{ section.settings.alignment }};">
  <form method="post" action="/contact" class="signup-box" id="contact_form" novalidate>
    <label for="email" class="signup-text">
      <h4>{{ section.settings.signup_text }}</h4>
    </label>
    <div class="main-form">
      <input
        type="email"
        name="contact[email]"
        id="email"
        placeholder="{{ section.settings.placeholder_text }}"
        required
      />
      <input type="hidden" name="form_type" value="customer">
      <input type="hidden" name="utf8" value="✓">
      <button type="submit" class="{{ section.settings.button_class }}">
        {{ section.settings.button_text }}
      </button>
    </div>
  </form>
</div>

<style>


  .newsletter {
    --bg-color: {{ section.settings.bg_color }};
    --text-color: {{ section.settings.text_color }};
    max-width:95%;
    margin:30px auto 0 auto;
    display: flex;
    justify-content: var(--align);
    --input-bg-color: {{ section.settings.input_bg_color }};
  }

  .newsletter .signup-box {
    background-color: var(--bg-color);
    border-radius: 6px;
    max-width: 350px;
  }

  .newsletter .signup-text {
    color: var(--text-color);
    margin-bottom: 12px;
  }

  .newsletter .main-form {
    display: flex;
    margin-top:5px;
    gap: 8px;
  }

  .newsletter input[type="email"] {
    flex: 1;
    padding: 8px;
    border: 1px solid var(--text-color);
    border-radius: 2px;
    background-color: var(--input-bg-color);
    color: var(--text-color);
    box-sizing: border-box;
  }

  .newsletter input::placeholder {
    color: var(--text-color);
  }
</style>

  <div class="footer-inner">
  <div class="footer__copyright">
    &copy;
    {{ 'now' | date: '%Y' }}
    {{ shop.name | link_to: routes.root_url }} LTD | CRN: 16666393 
  </div>

  <div class="footer__links">
    {% for link in section.settings.menu.links %}
      {{ link.title | link_to: link.url }}
    {% endfor %}
  </div>

      {% if section.settings.show_social %}
    <div class="footer__social">
      {% if section.settings.instagram_url != blank %}
        <a href="{{ section.settings.instagram_url }}" target="_blank" rel="noopener">Instagram</a>
      {% endif %}
      {% if section.settings.tiktok_url != blank %}
        <a href="{{ section.settings.tiktok_url }}" target="_blank" rel="noopener">TikTok</a>
      {% endif %}
    </div>
  {% endif %}


  </div>
  <div class="footer-wrap">
  <div class="footer-text">HIDDEN FUTURE</div>
  </div>

</footer>

{% stylesheet %}
.footer-wrap {
  width: 100%;
  height: 3.5rem;           /* controls how much is visible */
  overflow: hidden;         /* THIS causes the cut-off */
}

.footer-text {
  text-align: center;
  font-weight: 700;
  letter-spacing: 0.3em;
  font-size: clamp(2rem, 6vw, 6rem);
  transform: translateY(25%); /* pushes text downward */
}

  footer {
    margin-top: 0;
    border-top:1px solid var(--text-color);
  }
  .footer-inner {
    display: flex;
    padding-top: 2rem;
    padding-bottom: 2rem;
    max-width: 95%;
    margin: 0 auto;
    justify-content: space-between;
  }
  footer a {
    text-decoration: none;
    color: var(--text-color);
  }
  .footer__copyright {
    font-size: 12px;
    color: var(--text-color);
  }
  footer .footer__links,
  footer .footer__payment {
    display: flex;
    gap: 1rem;
    font-size: 12px;
    color: var(--text-color);
  }
  .footer__social {
    display: flex;
    gap: 1rem;
    font-size: 12px;
    color: var(--text-color);
  }

  /* Mobile stacked footer */
  @media (max-width: 680px) {
    .footer-inner {
      flex-direction: column;
      align-items: center;
      gap: 1.5rem;
      text-align: center;
    }
    footer .footer__links,
    footer .footer__payment,
    .footer__social {
      flex-direction: column;
      gap: 0.5rem;
      align-items: center;
    }
    .newsletter .main-form {
      max-width:100%;
      width:100%;
    }
  }
  /* Mobile stacked newsletter */
@media (max-width: 680px) {
  .newsletter {
    justify-content: center !important; /* Force center on mobile */
  }

  .newsletter .signup-box {
    margin: 0 auto; /* Center the box itself */
        max-width: 100% !important;
        width: 95%;
  }

  .newsletter .main-form {
    flex-direction: column; /* Stack input & button vertically if desired */
    width: 100%;
  }

  .newsletter input[type="email"],
  .newsletter button {
    width: 100%;
  }
}

{% endstylesheet %}


{% schema %}
{
  "name": "t:general.footer",
  "settings": [
    {
      "type": "header",
      "content": "Navigation"
    },
    {
      "type": "link_list",
      "id": "menu",
      "label": "t:labels.menu"
    },
    {
      "type": "checkbox",
      "id": "show_payment_icons",
      "label": "t:labels.show_payment_icons",
      "default": true
    },

    {
      "type": "header",
      "content": "Social Media"
    },
    {
      "type": "checkbox",
      "id": "show_social",
      "label": "Show social links",
      "default": false
    },
    {
      "type": "url",
      "id": "instagram_url",
      "label": "Instagram URL"
    },
    {
      "type": "url",
      "id": "facebook_url",
      "label": "Facebook URL"
    },
    {
      "type": "url",
      "id": "youtube_url",
      "label": "YouTube URL"
    },
    {
      "type": "url",
      "id": "tiktok_url",
      "label": "TikTok URL"
    },
    {
      "type": "url",
      "id": "pinterest_url",
      "label": "Pinterest URL"
    },

    {
      "type": "header",
      "content": "Newsletter Text & Styling"
    },
    {
      "type": "text",
      "id": "signup_text",
      "label": "Signup Text",
      "default": "Stay updated"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text Color",
      "default": "#000000"
    },
    {
      "type": "color",
      "id": "bg_color",
      "label": "Background Color",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "input_bg_color",
      "label": "Input Background Color",
      "default": "#f8f8f8"
    },
    {
      "type": "text",
      "id": "placeholder_text",
      "label": "Input Placeholder Text",
      "default": "Enter your email"
    },

    {
      "type": "header",
      "content": "Newsletter Settings"
    },
    {
      "type": "select",
      "id": "button_class",
      "label": "Button Class",
      "default": "fract-primary",
      "options": [
        {
          "value": "fract-primary",
          "label": "Fract Primary"
        },
        {
          "value": "fract-secondary",
          "label": "Fract Secondary"
        }
      ]
    },
    {
      "type": "text",
      "id": "button_text",
      "label": "Button Text",
      "default": "Submit"
    },
    {
      "type": "select",
      "id": "alignment",
      "label": "Form Alignment",
      "default": "center",
      "options": [
        {
          "value": "flex-start",
          "label": "Left"
        },
        {
          "value": "center",
          "label": "Center"
        },
        {
          "value": "flex-end",
          "label": "Right"
        }
      ]
    }
  ]
}
{% endschema %}

